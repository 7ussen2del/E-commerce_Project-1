## Install Docker And Start it 
- name: Install Docker
  ansible.builtin.dnf:
    name: docker
    state: present

- name: Enable Docker
  ansible.builtin.systemd:
    name: docker
    enabled: true

- name: Start Docker
  ansible.builtin.systemd:
    name: docker
    state: started
